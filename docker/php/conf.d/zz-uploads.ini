; Mounted into the PHP container — takes effect after: docker compose up -d php
; post_max_size must be >= the full multipart body (audio + image + form fields).
upload_max_filesize = 128M
post_max_size = 128M
